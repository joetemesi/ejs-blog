<%- include('partials/header'); -%>

<h1>Home</h1>
<p><%= startingContent %></p>

 <!-- for (i = 0; i < composeObject.length; i++) {  -->

    <% composeObject.forEach(function(composeObject){ %>
        <h1><%= composeObject.composeTitle %></h1>
        <p>
            <%= truncateText(composeObject.composePost, 100) %>... <a href='/posts/<%= encodeURIComponent(composeObject.composeTitle.toLowerCase()) %>'>Read More</a>
        </p>
    <% }) %>
        
<%- include('partials/footer'); -%>